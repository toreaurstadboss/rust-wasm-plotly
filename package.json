{
  "name": "rust-wasm-plotly",
  "version": "0.1.0",
  "scripts": {
    "dev": "cross-env NODE_OPTIONS= vite",
    "build:wasm": "wasm-pack build rust --target web --out-dir pkg"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "plotly.js-dist-min": "^2.35.3",
    "vite": "^5.0.0"
  }
}
